# NPM Configuration for bizapps-tasks

# Save exact versions (matching MJ pattern)
save-exact=true

# Use lockfile
package-lock=true

# Allow flexible peer dependency resolution (required for Angular + PrimeNG workspace)
legacy-peer-deps=true

# Strict SSL
strict-ssl=true
