---
"@mj-biz-apps/tasks-entities": minor
---

Task form uses left-nav. Both Task Dependencies grids (TaskID and DependsOnTaskID) are None — the sub-tasks gantt already plots those links. Tag links and notification logs are None. Created-by Tasks on Person are None.
