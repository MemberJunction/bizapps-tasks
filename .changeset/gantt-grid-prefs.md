---
"@mj-biz-apps/tasks-ng": patch
---

Tasks Gantt persists grid pane width and column widths with zoom in `mj.tasks.gantt.v1` (still reads the old zoom-only key). Other apps keep their own settings.
